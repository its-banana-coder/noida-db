name: ci

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    services:
      # Reference server for the differential tests (tests/redis_diff.rs).
      redis:
        image: redis:7.2
        ports: ["6379:6379"]
    env:
      NOIDA_REDIS_REF: 127.0.0.1:6379
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - run: cargo fmt --check
      - run: cargo clippy --all-targets -- -D warnings
      - run: cargo test
      # Show how many replies were compared against real Redis 7.2.
      - run: cargo test --test redis_diff -- --nocapture
      # Modularity: the binary must build with any service set, even none.
      - run: cargo build --no-default-features
      - run: scripts/check-size.sh
